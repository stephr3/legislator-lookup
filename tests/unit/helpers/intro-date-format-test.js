import { introDateFormat } from 'legislator-lookup/helpers/intro-date-format';
import { module, test } from 'qunit';

module('Unit | Helper | intro date format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = introDateFormat([42]);
  assert.ok(result);
});

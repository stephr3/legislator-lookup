import { chamberType } from 'legislator-lookup/helpers/chamber-type';
import { module, test } from 'qunit';

module('Unit | Helper | chamber type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = chamberType([42]);
  assert.ok(result);
});

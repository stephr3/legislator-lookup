import { subcommitteeNumber } from 'legislator-lookup/helpers/subcommittee-number';
import { module, test } from 'qunit';

module('Unit | Helper | subcommittee number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subcommitteeNumber([42]);
  assert.ok(result);
});
